node_modules
.next
out
build
dist
coverage
*.log
.env
.env.local
.DS_Store
data/*.db
data/*.db
next-env.d.ts
tsconfig.tsbuildinfo
# Secrets / local config: .env is never committed — copy .env.example instead.
# (Every value also has a safe default in src/lib/env.ts, so `npm run dev` works even without it.)
.env.local
.env.production
.env*.local
data/
data/
public/uploads/*
!public/uploads/.gitkeep
!public/uploads/README.md
