# Uploads

Files uploaded from the admin panel (lesson recordings, handbooks, practice packs) land here under `uploads/<yyyy-mm>/` and are served statically by Next.

They are git-ignored — keep the directory itself with `.gitkeep`.
